{
  "name": "ships-demo",
  "version": "1.0.0",
  "description": "Nest TypeScript starter repository",
  "license": "MIT",
  "main": "index.js",
  "scripts": {
    "start": "npm run build && node ./start.js",
    "start:watch": "npm run build && ./node_modules/.bin/tsc-watch --onSuccess \"node ./start.js\"",
    "test": "npm run build && node ./test.js",
    "test:watch": "npm run build && ./node_modules/.bin/tsc-watch --onSuccess \"node ./test.js\"",
    "build": "./node_modules/.bin/tsc && cp ./specs/jasmine.json ./dist/specs/jasmine.json"
  },
  "dependencies": {
    "reflect-metadata": "^0.1.10",
    "source-map-support": "^0.5.4",
    "tsc-watch": "^1.0.17",
    "tsconfig-paths": "^3.2.0",
    "tspath": "^1.2.10",
    "typescript": "^2.8.1"
  },
  "devDependencies": {
    "@types/jasmine": "^2.8.3",
    "@types/node": "^8.0.28",
    "jasmine": "^2.8.0"
  }
}
